# 🌿 Git Flow - Reforma

## 📋 Ramas del Proyecto

### **master**
Rama principal con el código estable en producción
- ✅ Código probado y listo para producción
- ✅ Despliegue automático
- ⚠️ Solo merges desde `testing` después de QA completo

### **development**
Rama de desarrollo activo
- 🚧 Código en desarrollo continuo
- 🚧 Nuevas features
- 🚧 Correcciones de bugs
- 🔄 Merges frecuentes

### **testing**
Rama para testing y QA
- 🧪 Código que pasa de development
- 🧪 Testing funcional
- 🧪 QA y validaciones
- 🔄 Solo merges aprobados a `master`

### **production**
Rama de producción (igual a master)
- 🚀 Código en producción
- 🚀 Monitorización activa
- 🚀 Rollbacks si es necesario

## 🔄 Flujo de Trabajo

### Desarrollo de Features

1. **Crear feature desde development:**
```bash
git checkout development
git checkout -b feature/nueva-funcionalidad
# ... desarrollar
git commit -m "feat: nueva funcionalidad"
git push origin feature/nueva-funcionalidad
```

2. **Merge a development:**
```bash
git checkout development
git merge feature/nueva-funcionalidad
git push origin development
```

### Testing

3. **Deploy a Testing:**
```bash
git checkout testing
git merge development
git push origin testing
```
- Render/Vercel despliega automáticamente la rama `testing`

### Producción

4. **Deploy a Producción:**
```bash
# Solo después de QA completo
git checkout master
git merge testing
git push origin master

# Actualizar producción
git checkout production
git merge master
git push origin production
```

## 📊 Deployment Automático

### Configuración en Render/Vercel:

**Backend (Render):**
- Branch: `development` para desarrollo
- Branch: `testing` para testing
- Branch: `master` para producción

**Frontend (Vercel):**
- Branch: `development` para desarrollo  
- Branch: `testing` para testing
- Branch: `master` para producción

## ⚠️ Reglas Importantes

1. **master**: SOLO merges desde `testing`
2. **testing**: SOLO merges desde `development`
3. **development**: Merges desde features
4. **production**: Idéntica a `master`

## 🔐 Protección de Ramas

### En GitHub:

1. Ve a Settings → Branches
2. Agrega rules para `master` y `production`:
   - Requiere PR approvals
   - Requiere status checks
   - No permite push directo
   - Requires conversations to resolve

## 📝 Convenciones de Commits

```
feat: Nueva funcionalidad
fix: Corrección de bug
docs: Actualización de documentación
style: Cambios de formato (no afectan código)
refactor: Refactorización de código
test: Agregar tests
chore: Tareas de mantenimiento
```

## 🚀 Comandos Útiles

```bash
# Ver ramas
git branch -a

# Cambiar de rama
git checkout development

# Actualizar rama local
git pull origin development

# Ver diferencias entre ramas
git diff master..development

# Ver historial de una rama
git log development --oneline

# Merge sin fast-forward
git merge --no-ff feature/nueva-funcionalidad
```

## ✅ Estado Actual

- ✅ master: Código base subido
- ✅ development: Igual a master (listo para desarrollo)
- ✅ testing: Vacía (listo para QA)
- ✅ production: Vacía (listo para producción)

## 🎯 Próximos Pasos

1. Trabajar en `development`
2. Probar en `testing`
3. Deployar desde `master` a `production`

